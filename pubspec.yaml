name: pixabay_gallery
description: "A Flutter application that displays a gallery of images from Pixabay"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  cached_network_image: ^3.4.1
  collection: ^1.18.0
  dio: ^5.7.0
  flutter:
    sdk: flutter
  get: ^4.6.6
  infinite_scroll_pagination: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
